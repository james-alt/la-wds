<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="{{ lawds.path }}/styles/la.css">
        
        <title>{{ _self.title }}</title>
    </head>
    <body>
    {% block body %}{{ yield }}{% endblock %}
    </body>
</html>